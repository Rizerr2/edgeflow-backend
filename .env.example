# EdgeFlow Backend Environment Variables
# Copy these variable names to Railway and set real values

# Secret token for mentor to send signals (make this random and strong)
MENTOR_TOKEN=your-secret-token-here

# Comma-separated list of valid license keys
LICENSE_KEYS=key1,key2,key3

# Port (Railway sets this automatically, don't change)
PORT=3000
